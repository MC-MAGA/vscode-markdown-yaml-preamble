> [!WARNING]
> This extension is deprecated because this functionality is now built into VS Code 1.121+.

Makes yaml front matter render as a table in VS Code's built-in markdown preview:

![Example preamble rendering as a table](https://raw.githubusercontent.com/mjbvz/vscode-markdown-yaml-preamble/master/docs/example.png)

# Features 
- Adds support for rendering yaml front matter as a table in VS Code's built-in markdown preview.
